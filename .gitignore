# never commit secrets
secret.txt
*.secret
.env
.env.*

# plaintext source of the site (letter + passcode + Supabase key in the clear)
# — only the ENCRYPTED index.html gets published
index.src.html

# macOS
.DS_Store

# full local backup of everything (never published)
backup/

# local archive of retired screens (never published)
store.html
extract-store.js
